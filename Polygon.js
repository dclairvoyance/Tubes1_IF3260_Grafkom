class Polygon extends Shape {
    constructor(id, type, vertices, colors, theta) {
        super(id, type);
        this.vertices = vertices;
        this.colors = colors;
        this.theta = theta;
    }

    center() {
        Math.det([1, 2], [3, 4]);
        for (let i = 0; i < this.vertices.length; i++) {

        }
    }
}